import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
    wrap:{
        alignItems: "center",
        flexDirection: 'row',
        justifyContent: "space-between",
        marginBottom: 10
    } 
})