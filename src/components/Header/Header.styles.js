import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
    wrap:{
        flexDirection: 'row',
        justifyContent: "space-between",
        alignItems: "center"
    } 
})